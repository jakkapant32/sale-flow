# วิธีติดตั้ง PostgreSQL Driver สำหรับ XAMPP

## ❌ ปัญหา: "could not find driver"

ข้อผิดพลาดนี้เกิดจาก PHP ไม่มี PostgreSQL PDO driver (pdo_pgsql) ติดตั้งอยู่

---

## ✅ วิธีแก้ไข

### วิธีที่ 1: เปิดใช้งาน Extension ใน php.ini (แนะนำ)

#### ขั้นตอนที่ 1: เปิดไฟล์ php.ini
1. เปิด XAMPP Control Panel
2. กดปุ่ม **Config** ที่ Apache
3. เลือก **PHP (php.ini)**
4. หรือเปิดไฟล์โดยตรง: `C:\xampp\php\php.ini`

#### ขั้นตอนที่ 2: ค้นหาและเปิดใช้งาน extension
1. กด `Ctrl + F` เพื่อค้นหา
2. พิมพ์: `pdo_pgsql`
3. หาบรรทัดที่มี:
   ```ini
   ;extension=pdo_pgsql
   ```
4. **เอา semicolon (;) ออก** เปลี่ยนเป็น:
   ```ini
   extension=pdo_pgsql
   ```
5. บันทึกไฟล์ (Ctrl + S)

#### ขั้นตอนที่ 3: ตรวจสอบไฟล์ DLL
ตรวจสอบว่ามีไฟล์ `php_pdo_pgsql.dll` อยู่ใน:
```
C:\xampp\php\ext\php_pdo_pgsql.dll
```

#### ขั้นตอนที่ 4: Restart Apache
1. กลับไปที่ XAMPP Control Panel
2. กด **Stop** ที่ Apache
3. รอสักครู่
4. กด **Start** ที่ Apache อีกครั้ง

#### ขั้นตอนที่ 5: ตรวจสอบ
เปิดเบราว์เซอร์ไปที่:
```
http://localhost/SalesFlow/check_php_extensions.php
```

---

### วิธีที่ 2: ถ้าไม่มีไฟล์ DLL

#### ตัวเลือก A: ดาวน์โหลด DLL จาก PECL
1. ไปที่: https://pecl.php.net/package/pdo_pgsql
2. ดาวน์โหลด DLL ที่ตรงกับ PHP version ของคุณ
3. วางไฟล์ DLL ใน: `C:\xampp\php\ext\`
4. ทำตามวิธีที่ 1

#### ตัวเลือก B: ติดตั้ง PostgreSQL Client Libraries
1. ดาวน์โหลด PostgreSQL จาก: https://www.postgresql.org/download/windows/
2. ติดตั้ง PostgreSQL (เลือกเฉพาะ client libraries)
3. เพิ่ม path ของ PostgreSQL bin ใน System PATH:
   ```
   C:\Program Files\PostgreSQL\15\bin
   ```
4. Restart Apache

#### ตัวเลือก C: ใช้ XAMPP ที่มี PostgreSQL Support
- บางเวอร์ชันของ XAMPP มี PostgreSQL support อยู่แล้ว
- ลองอัปเดต XAMPP เป็นเวอร์ชันล่าสุด

---

### วิธีที่ 3: ใช้ PHP Built-in Server (ไม่ต้องใช้ XAMPP)

ถ้า XAMPP ไม่รองรับ PostgreSQL สามารถใช้ PHP standalone:

1. ดาวน์โหลด PHP จาก: https://windows.php.net/download/
2. ติดตั้ง PostgreSQL client libraries
3. เปิดใช้งาน extension ใน php.ini
4. ใช้ PHP built-in server:
   ```cmd
   cd C:\xampp\htdocs\SalesFlow
   php -S localhost:8000
   ```
5. เปิดเบราว์เซอร์: `http://localhost:8000`

---

## 🔍 ตรวจสอบการติดตั้ง

### วิธีที่ 1: ใช้ไฟล์ตรวจสอบ
เปิดเบราว์เซอร์ไปที่:
```
http://localhost/SalesFlow/check_php_extensions.php
```

### วิธีที่ 2: ใช้ PHP CLI
เปิด Command Prompt:
```cmd
cd C:\xampp\php
php -m | findstr pdo_pgsql
```

ถ้าเห็น `pdo_pgsql` แสดงว่าติดตั้งสำเร็จ ✅

### วิธีที่ 3: ใช้ phpinfo()
สร้างไฟล์ `info.php`:
```php
<?php
phpinfo();
?>
```

เปิดเบราว์เซอร์: `http://localhost/info.php`
ค้นหา "pdo_pgsql" ในหน้า

---

## ⚠️ ปัญหาที่พบบ่อย

### ปัญหา: "The specified module could not be found"
**แก้ไข:**
- ตรวจสอบว่าไฟล์ DLL อยู่ในโฟลเดอร์ `ext`
- ตรวจสอบว่า PHP version ตรงกับ DLL version (Thread Safe vs Non-Thread Safe)
- ติดตั้ง PostgreSQL client libraries

### ปัญหา: "Call to undefined function"
**แก้ไข:**
- ตรวจสอบว่าเอา semicolon (;) ออกจาก php.ini แล้ว
- Restart Apache
- ตรวจสอบว่า extension ถูกโหลดด้วย `php -m`

### ปัญหา: "Extension already loaded"
**แก้ไข:**
- ตรวจสอบว่าไม่มี extension ถูกโหลดซ้ำใน php.ini
- ตรวจสอบไฟล์ php.ini ทั้งหมด (อาจมีหลายไฟล์)

---

## 📝 หมายเหตุ

- หลังจากแก้ไข php.ini **ต้อง Restart Apache ทุกครั้ง**
- ตรวจสอบ PHP version ให้ตรงกับ DLL version
- ถ้าใช้ XAMPP 64-bit ต้องใช้ DLL 64-bit
- ถ้าใช้ XAMPP 32-bit ต้องใช้ DLL 32-bit

---

## ✅ หลังจากติดตั้งสำเร็จ

1. ลบไฟล์ `check_php_extensions.php` (เพื่อความปลอดภัย)
2. ลองสมัครสมาชิกอีกครั้ง
3. ระบบควรทำงานได้ปกติ



