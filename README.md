# SalesFlow

SalesFlow - CRM System for managing customers, deals, activities, products, and orders. Built with PHP and PostgreSQL.

## ฟีเจอร์หลัก

- 👥 **จัดการลูกค้า** - เก็บข้อมูลลูกค้าแบบครบถ้วน
- 💼 **จัดการดีล** - ติดตามโอกาสขายและสถานะดีล
- 📅 **จัดการกิจกรรม** - บันทึกและติดตามกิจกรรมทั้งหมด
- 📦 **จัดการสินค้า** - จัดการสินค้าและสต็อก
- 🛒 **จัดการคำสั่งซื้อ** - สร้างและจัดการคำสั่งซื้อ
- 📊 **แดชบอร์ด** - ดูสถิติและรายงานแบบเรียลไทม์

## ความต้องการของระบบ

- PHP 8.0 หรือสูงกว่า
- PostgreSQL 12 หรือสูงกว่า
- Apache Web Server (พร้อม mod_rewrite) หรือ Nginx
- PDO PostgreSQL Extension

## การติดตั้ง

### 1. ติดตั้งฐานข้อมูล

เชื่อมต่อกับฐานข้อมูล PostgreSQL และรันไฟล์ schema:

```bash
psql -h your-host -U your-user -d your-database < database/schema.sql
```

หรือใช้ pgAdmin หรือเครื่องมือจัดการฐานข้อมูลอื่นๆ เพื่อรันไฟล์ `database/schema.sql`

### 2. ตั้งค่าไฟล์

ไฟล์ `config/database.php` รองรับการอ่านจาก environment variables สำหรับ production หรือใช้ค่า default สำหรับ local development:

**สำหรับ Production (Render.com):**
- ตั้งค่า `DATABASE_URL` หรือตัวแปร `DB_HOST`, `DB_PORT`, `DB_NAME`, `DB_USER`, `DB_PASSWORD` ใน Environment Variables

**สำหรับ Local Development:**
- แก้ไขค่าใน `config/database.php` โดยตรง

### 3. ตั้งค่า Web Server

#### สำหรับ Apache:
- ตรวจสอบว่า mod_rewrite เปิดใช้งาน
- ไฟล์ `.htaccess` จะจัดการ routing อัตโนมัติ

#### สำหรับ Nginx:
เพิ่ม configuration นี้ใน server block:

```nginx
location /api {
    try_files $uri $uri/ /api/index.php?path=$uri&$args;
}

location ~ \.php$ {
    fastcgi_pass unix:/var/run/php/php8.0-fpm.sock;
    fastcgi_index index.php;
    include fastcgi_params;
    fastcgi_param SCRIPT_FILENAME $document_root$fastcgi_script_name;
}
```

### 4. เริ่มใช้งาน

1. เปิดเบราว์เซอร์ไปที่ `http://your-domain/`
2. สมัครสมาชิกใหม่หรือใช้บัญชีที่มีอยู่

## การ Deploy บน Render.com

ดูรายละเอียดการ deploy ได้ที่ [README_DEPLOY.md](README_DEPLOY.md)

## โครงสร้างโปรเจกต์

```
SalesFlow/
├── api/                    # API endpoints
│   ├── index.php          # Main API router
│   ├── auth.php           # Authentication API
│   ├── customers.php      # Customers API
│   ├── deals.php          # Deals API
│   ├── activities.php     # Activities API
│   ├── products.php       # Products API
│   ├── orders.php         # Orders API
│   ├── dashboard.php      # Dashboard API
│   └── users.php          # Users API
├── assets/
│   └── css/
│       └── main.css       # Main stylesheet
├── config/
│   ├── database.php       # Database configuration
│   └── functions.php      # Helper functions
├── database/
│   └── schema.sql         # Database schema
├── includes/
│   └── header.php         # Common header
├── index.php              # Login page
├── register.php           # Registration page
├── dashboard.php          # Dashboard page
├── customers.php          # Customers management
├── deals.php              # Deals management
├── activities.php         # Activities management
├── products.php           # Products management
├── orders.php             # Orders management
├── .htaccess              # Apache rewrite rules
├── render.yaml            # Render.com configuration
└── README.md              # This file
```

## API Endpoints

### Authentication
- `POST /api/auth/login` - เข้าสู่ระบบ
- `POST /api/auth/register` - สมัครสมาชิก
- `POST /api/auth/logout` - ออกจากระบบ
- `GET /api/auth/me` - ข้อมูลผู้ใช้ปัจจุบัน

### Customers
- `GET /api/customers` - ดึงรายการลูกค้า
- `GET /api/customers/{id}` - ดึงข้อมูลลูกค้า
- `POST /api/customers` - สร้างลูกค้าใหม่
- `PUT /api/customers/{id}` - อัปเดตลูกค้า
- `DELETE /api/customers/{id}` - ลบลูกค้า

### Deals
- `GET /api/deals` - ดึงรายการดีล
- `GET /api/deals/{id}` - ดึงข้อมูลดีล
- `POST /api/deals` - สร้างดีลใหม่
- `PUT /api/deals/{id}` - อัปเดตดีล
- `DELETE /api/deals/{id}` - ลบดีล

### Activities
- `GET /api/activities` - ดึงรายการกิจกรรม
- `GET /api/activities/{id}` - ดึงข้อมูลกิจกรรม
- `POST /api/activities` - สร้างกิจกรรมใหม่
- `PUT /api/activities/{id}` - อัปเดตกิจกรรม
- `DELETE /api/activities/{id}` - ลบกิจกรรม

### Products
- `GET /api/products` - ดึงรายการสินค้า
- `GET /api/products/{id}` - ดึงข้อมูลสินค้า
- `POST /api/products` - สร้างสินค้าใหม่
- `PUT /api/products/{id}` - อัปเดตสินค้า
- `DELETE /api/products/{id}` - ลบสินค้า

### Orders
- `GET /api/orders` - ดึงรายการคำสั่งซื้อ
- `GET /api/orders/{id}` - ดึงข้อมูลคำสั่งซื้อ
- `POST /api/orders` - สร้างคำสั่งซื้อใหม่
- `PUT /api/orders/{id}` - อัปเดตคำสั่งซื้อ
- `DELETE /api/orders/{id}` - ลบคำสั่งซื้อ

### Dashboard
- `GET /api/dashboard` - ดึงสถิติแดชบอร์ด
- `GET /api/dashboard?period=year` - ดึงสถิติรายปี
- `GET /api/dashboard?period=quarter` - ดึงสถิติรายไตรมาส
- `GET /api/dashboard?period=month` - ดึงสถิติรายเดือน

## การใช้งาน

### การจัดการลูกค้า
1. ไปที่เมนู "ลูกค้า"
2. คลิก "เพิ่มลูกค้าใหม่" เพื่อเพิ่มลูกค้า
3. กรอกข้อมูลที่จำเป็น (ชื่อ, นามสกุล)
4. บันทึกข้อมูล

### การจัดการดีล
1. ไปที่เมนู "ดีล"
2. คลิก "เพิ่มดีลใหม่"
3. เลือกลูกค้าและกรอกมูลค่าดีล
4. เลือกขั้นตอน (Stage) และโอกาสสำเร็จ (Probability)
5. บันทึกข้อมูล

### การจัดการกิจกรรม
1. ไปที่เมนู "กิจกรรม"
2. คลิก "เพิ่มกิจกรรมใหม่"
3. เลือกประเภทกิจกรรม (โทรศัพท์, ประชุม, อีเมล, งาน)
4. ระบุวันที่ครบกำหนดและความสำคัญ
5. บันทึกข้อมูล

### การจัดการคำสั่งซื้อ
1. ไปที่เมนู "คำสั่งซื้อ"
2. คลิก "เพิ่มคำสั่งซื้อใหม่"
3. เลือกลูกค้าและเพิ่มสินค้า
4. ระบุจำนวนและราคา
5. บันทึกคำสั่งซื้อ

## ความปลอดภัย

⚠️ **คำเตือน:**
- เปลี่ยนรหัสผ่าน default ของ admin ทันทีหลังติดตั้ง
- ควรตั้งค่า HTTPS สำหรับ production
- ควรเพิ่มการตรวจสอบสิทธิ์ที่เข้มงวดขึ้น
- ควรใช้ prepared statements (ซึ่งระบบใช้อยู่แล้ว)
- ควรตั้งค่า CORS ให้เหมาะสมสำหรับ production
- อย่า hardcode credentials ใน code - ใช้ environment variables แทน

## การแก้ปัญหา

### ไม่สามารถเชื่อมต่อฐานข้อมูลได้
- ตรวจสอบข้อมูลการเชื่อมต่อใน `config/database.php` หรือ Environment Variables
- ตรวจสอบว่า PostgreSQL extension เปิดใช้งานใน PHP
- ตรวจสอบว่าไฟร์วอลล์อนุญาตให้เชื่อมต่อฐานข้อมูล

### API ไม่ทำงาน
- ตรวจสอบว่า mod_rewrite เปิดใช้งาน (Apache)
- ตรวจสอบไฟล์ `.htaccess`
- ดู error logs ของ web server

### หน้าเว็บไม่แสดงผลถูกต้อง
- ตรวจสอบว่า path ของ CSS ถูกต้อง
- ตรวจสอบ console ของ browser สำหรับ JavaScript errors
- ตรวจสอบว่า PHP sessions ทำงานได้

## License

โปรเจกต์นี้สร้างขึ้นเพื่อการศึกษาและใช้งานส่วนตัว

## Support

หากมีปัญหาหรือคำถาม กรุณาตรวจสอบ documentation หรือติดต่อทีมพัฒนา
