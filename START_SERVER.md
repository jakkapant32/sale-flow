# วิธีเริ่ม Web Server สำหรับ CRM System

## ❌ ปัญหา: ERR_CONNECTION_REFUSED

ข้อผิดพลาดนี้หมายความว่า Web Server ยังไม่ได้เริ่มทำงาน

---

## ✅ วิธีแก้ไข: เริ่ม Web Server

### วิธีที่ 1: ใช้ XAMPP (แนะนำ - ง่ายที่สุด)

#### 1. ดาวน์โหลดและติดตั้ง XAMPP
- ดาวน์โหลดจาก: https://www.apachefriends.org/download.html
- ติดตั้ง XAMPP (แนะนำให้ติดตั้งใน `C:\xampp`)

#### 2. เริ่ม Apache
1. เปิด **XAMPP Control Panel**
2. กดปุ่ม **Start** ที่ Apache
3. รอจน Status เปลี่ยนเป็น **Running** (สีเขียว)

#### 3. วางไฟล์โปรเจกต์
- Copy โฟลเดอร์ `SalesFlow` ทั้งหมดไปที่:
  ```
  C:\xampp\htdocs\SalesFlow
  ```

#### 4. เปิดเบราว์เซอร์
```
http://localhost/SalesFlow/install.php
```

---

### วิธีที่ 2: ใช้ WAMP Server

#### 1. ดาวน์โหลดและติดตั้ง WAMP
- ดาวน์โหลดจาก: https://www.wampserver.com/en/download-wampserver-64bits/

#### 2. เริ่ม WAMP
1. เปิด **WAMP Server**
2. รอจนไอคอนเป็นสีเขียว (สีเขียว = ทำงาน)
3. ถ้ายังเป็นสีเหลือง/แดง ให้คลิกขวา → "Put Online"

#### 3. วางไฟล์โปรเจกต์
- Copy โฟลเดอร์ `SalesFlow` ไปที่:
  ```
  C:\wamp64\www\SalesFlow
  ```

#### 4. เปิดเบราว์เซอร์
```
http://localhost/SalesFlow/install.php
```

---

### วิธีที่ 3: ใช้ PHP Built-in Server (ไม่ต้องติดตั้ง XAMPP/WAMP)

#### ข้อกำหนด:
- ต้องติดตั้ง PHP ก่อน (ดาวน์โหลดจาก https://windows.php.net/download/)

#### 1. เปิด Command Prompt หรือ PowerShell
- กด `Win + R` → พิมพ์ `cmd` → Enter

#### 2. ไปที่โฟลเดอร์โปรเจกต์
```cmd
cd C:\Users\User\OneDrive\Desktop\SalesFlow
```

#### 3. เริ่ม PHP Server
```cmd
php -S localhost:8000
```

#### 4. เปิดเบราว์เซอร์
```
http://localhost:8000/install.php
```

---

### วิธีที่ 4: ใช้ Laragon (สำหรับ PHP Developers)

#### 1. ดาวน์โหลด Laragon
- ดาวน์โหลดจาก: https://laragon.org/download/

#### 2. เริ่ม Laragon
- Laragon จะเริ่ม Apache และ MySQL อัตโนมัติ

#### 3. วางไฟล์โปรเจกต์
- Copy โฟลเดอร์ `SalesFlow` ไปที่:
  ```
  C:\laragon\www\SalesFlow
  ```

---

## 🔍 ตรวจสอบว่า Web Server ทำงานอยู่หรือไม่

### วิธีตรวจสอบ:

1. **เปิดเบราว์เซอร์** ไปที่:
   ```
   http://localhost
   ```
   
2. **ถ้าเห็นหน้า XAMPP/WAMP** = Web Server ทำงาน ✅
3. **ถ้าเห็น ERR_CONNECTION_REFUSED** = Web Server ไม่ได้ทำงาน ❌

---

## ⚠️ หมายเหตุสำคัญ

### สำหรับ PostgreSQL:
- Web Server (Apache/PHP) ใช้สำหรับรัน PHP code
- **ไม่ต้องใช้** MySQL/MariaDB (เราใช้ PostgreSQL บน Render.com)
- แค่ให้ PHP และ Apache ทำงานก็พอ

---

## 🚀 ทางเลือกอื่น: ติดตั้งฐานข้อมูลโดยไม่ใช้ Web Server

ถ้าไม่อยากติดตั้ง Web Server ตอนนี้ สามารถใช้:

### ใช้ pgAdmin (แนะนำ):
1. ดาวน์โหลด: https://www.pgadmin.org/download/
2. เชื่อมต่อกับฐานข้อมูล Render.com
3. เปิดไฟล์ `database/reset_and_install.sql` และรัน

### ใช้ DBeaver:
1. ดาวน์โหลด: https://dbeaver.io/download/
2. เชื่อมต่อกับฐานข้อมูล Render.com
3. เปิดไฟล์ `database/reset_and_install.sql` และรัน

---

## ✅ หลังจากเริ่ม Web Server แล้ว

1. เปิดเบราว์เซอร์ไปที่:
   ```
   http://localhost/SalesFlow/install.php
   ```
   หรือ
   ```
   http://localhost:8000/install.php
   ```
   (ถ้าใช้ PHP built-in server)

2. กดปุ่ม **"ติดตั้งฐานข้อมูล"**

3. รอให้เสร็จ

4. **ลบไฟล์ `install.php`** เพื่อความปลอดภัย

5. เข้าสู่ระบบที่:
   ```
   http://localhost/SalesFlow/
   ```
   Username: `admin`  
   Password: `admin123`

