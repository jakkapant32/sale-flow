# 📘 คู่มือติดตั้งและใช้งาน pgAdmin 4

## ✅ ขั้นตอนที่ 1: ดาวน์โหลด pgAdmin

1. **เลือกเวอร์ชัน:**
   - แนะนำ: **pgAdmin 4 v9.11** (ล่าสุด)
   - หรือเวอร์ชันที่รองรับ Windows ของคุณ

2. **คลิกที่ลิงก์ดาวน์โหลด** (จะดาวน์โหลดไฟล์ .exe)

---

## ✅ ขั้นตอนที่ 2: ติดตั้ง pgAdmin

1. **รันไฟล์ที่ดาวน์โหลด** (เช่น `pgadmin4-9.11-x64.exe`)

2. **ทำตามขั้นตอนติดตั้ง:**
   - กด "Next" → "I Agree" (ยอมรับเงื่อนไข)
   - เลือกโฟลเดอร์ติดตั้ง (แนะนำค่า default)
   - กด "Next" → "Next" → "Install"
   - รอให้ติดตั้งเสร็จ → กด "Finish"

3. **เปิด pgAdmin 4:**
   - pgAdmin จะเปิดขึ้นมาอัตโนมัติ
   - หรือหาจาก Start Menu: "pgAdmin 4"

4. **ตั้งรหัสผ่าน Master Password (ครั้งแรก):**
   - pgAdmin จะถามรหัสผ่าน Master Password
   - **ตั้งรหัสผ่านที่จำง่ายแต่ปลอดภัย** (ใช้สำหรับเปิด pgAdmin)
   - **จำรหัสนี้ไว้!** (ใช้ทุกครั้งที่เปิด pgAdmin)

---

## ✅ ขั้นตอนที่ 3: เชื่อมต่อฐานข้อมูล Render.com

### 1. สร้าง Server Connection

1. **คลิกขวาที่ "Servers"** (ทางซ้าย) → **"Create"** → **"Server..."**

2. **General Tab:**
   - **Name:** `Render CRM` (ชื่ออะไรก็ได้ที่คุณจำได้)

3. **Connection Tab:**
   - **Host name/address:** 
     ```
     dpg-d51mhfggjchc73enlnfg-a.oregon-postgres.render.com
     ```
   - **Port:** `5432`
   - **Maintenance database:** `smartsales_db_tp3c`
   - **Username:** `smartsales_user`
   - **Password:** `p0vTgAP02R8i8hKXjPF5uWpwDsE1nZr4`
   - ✅ **ติ๊กถูกที่ "Save password"** (เพื่อไม่ต้องกรอกใหม่ทุกครั้ง)

4. **กด "Save"**

### 2. ตรวจสอบการเชื่อมต่อ

- ถ้าเชื่อมต่อสำเร็จ จะเห็น:
  - ✅ "Render CRM" ปรากฏใต้ "Servers"
  - ✅ ขยายได้เห็น "Databases" → "smartsales_db_tp3c"

---

## ✅ ขั้นตอนที่ 4: รันสคริปต์ติดตั้งฐานข้อมูล

### 1. เปิด Query Tool

1. **ขยาย Servers → Render CRM → Databases**
2. **คลิกขวาที่ `smartsales_db_tp3c`** → **"Query Tool"**

### 2. เปิดไฟล์ SQL

1. **ใน Query Tool** → กด **File** → **Open File...**
2. **ไปที่โฟลเดอร์:** `C:\Users\User\OneDrive\Desktop\SalesFlow\database\`
3. **เลือกไฟล์:** `reset_and_install.sql`
4. **กด "Open"**

### 3. รันสคริปต์

1. **ตรวจสอบว่าเห็น SQL code ทั้งหมด** (ควรจะยาวมาก)
2. **กด F5** หรือคลิกปุ่ม **▶ Execute/Refresh** (Execute)
3. **รอให้รันเสร็จ** (อาจใช้เวลา 10-30 วินาที)

### 4. ตรวจสอบผลลัพธ์

- ด้านล่าง Query Tool จะเห็น:
  - ✅ "Query returned successfully" (สำเร็จ!)
  - หรือรายละเอียดของตารางที่สร้างแล้ว

---

## ✅ ขั้นตอนที่ 5: ตรวจสอบว่าติดตั้งสำเร็จ

### 1. ตรวจสอบตาราง

1. **ขยาย Servers → Render CRM → Databases → smartsales_db_tp3c → Schemas → public → Tables**

2. **ควรเห็นตารางทั้งหมด 7 ตาราง:**
   - ✅ `activities`
   - ✅ `customers`
   - ✅ `deals`
   - ✅ `order_items`
   - ✅ `orders`
   - ✅ `products`
   - ✅ `users`

### 2. ตรวจสอบข้อมูลเริ่มต้น

1. **คลิกขวาที่ตาราง `users`** → **"View/Edit Data"** → **"All Rows"**
2. **ควรเห็น 1 แถว:** 
   - username: `admin`
   - email: `admin@crm.local`

3. **คลิกขวาที่ตาราง `products`** → **"View/Edit Data"** → **"All Rows"**
4. **ควรเห็น 3 แถว:** Product A, Product B, Product C

---

## 🎉 สำเร็จแล้ว!

ฐานข้อมูลถูกติดตั้งเรียบร้อยแล้ว

### ข้อมูลเข้าสู่ระบบ:
- **Username:** `admin`
- **Password:** `admin123`

⚠️ **ควรเปลี่ยนรหัสผ่านทันทีหลังเข้าสู่ระบบครั้งแรก!**

---

## 🔧 แก้ไขปัญหา

### ❌ "Unable to connect to server"
- ตรวจสอบการเชื่อมต่ออินเทอร์เน็ต
- ตรวจสอบว่า Host, Port, Database name, Username, Password ถูกต้อง
- ลอง ping host name เพื่อดูว่าเชื่อมต่อได้หรือไม่

### ❌ "Password authentication failed"
- ตรวจสอบ Password ว่าใส่ถูกต้อง (copy-paste จากข้อมูลที่ให้มา)

### ❌ "Database does not exist"
- ตรวจสอบว่า Database name: `smartsales_db_tp3c` ถูกต้อง

### ❌ SQL Error เมื่อรันสคริปต์
- ตรวจสอบว่าเลือก database `smartsales_db_tp3c` ก่อนรันสคริปต์
- ลองรันใหม่ (บางครั้งอาจมีปัญหาจากการเชื่อมต่อชั่วคราว)

---

## 📝 คำแนะนำเพิ่มเติม

- **จำ Master Password ของ pgAdmin ไว้** (ใช้เปิด pgAdmin)
- **ไม่ต้องลบ pgAdmin** - ใช้จัดการฐานข้อมูลได้ในอนาคต
- **สำรองข้อมูลเป็นประจำ** โดยใช้ pgAdmin: คลิกขวาที่ database → "Backup..."

---

## 📞 ขั้นตอนถัดไป

หลังจากติดตั้งฐานข้อมูลเสร็จแล้ว:

1. **ติดตั้ง Web Server** (XAMPP/WAMP) เพื่อรันระบบ
2. **เปิดเบราว์เซอร์** ไปที่ `http://localhost/SalesFlow/`
3. **เข้าสู่ระบบ** ด้วย username: `admin`, password: `admin123`

